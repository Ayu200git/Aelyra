{
  "name": "aelyra",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
     "build": "echo 'Skipping root build on Railway",
    "install-all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.0.2"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}
